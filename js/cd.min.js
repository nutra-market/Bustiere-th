! function (i) {
    "use strict";
    var r = i.document,
        e = r.body,
        n = r.documentElement,
        o = i.requestAnimationFrame || i.mozRequestAnimationFrame || i.webkitRequestAnimationFrame || function (t) {
            i.setTimeout(t, 15)
        },
        c = "",
        s = 500,
        u = i,
        a = u.scrollTop || i.pageYOffset,
        f = 0,
        l = function (t, o, e, n) {
            return n < e ? o : t + (f - t) * ((l = e / n) < .5 ? 4 * l * l * l : (l - 1) * (2 * l - 2) * (2 * l - 2) + 1);
            var l
        },
        m = function () {
            var t = Date.now() - c;
            u === i ? i.scroll(0, l(a, f, t, s)) : u.scrollTop = l(a, f, t, s), t <= s && o(m)
        },
        t = function () {};
    t.prototype = {
        scrollTo: function (t, o, e) {
            var n, l;
            c = Date.now(), s = o || 500, a = (u = e || i).scrollTop || i.pageYOffset, l = {}, f = "number" == typeof (n = t) ? n : "string" == typeof n && !!(l = r.querySelector(n)) && l.getBoundingClientRect().top + i.pageYOffset, m()
        },
        scrollTop: function (t, o) {
            this.scrollTo(0, t, o)
        },
        scrollBottom: function (t, o) {
            this.scrollTo(Math.max.apply(null, [e.clientHeight, e.scrollHeight, n.scrollHeight, n.clientHeight]) - i.innerHeight, t, o)
        }
    }, i.smoothScroll = new t
}(window);

document.querySelectorAll('a[href^="#"]').forEach(function (anchor) {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        smoothScroll.scrollTo(this.getAttribute('href'), 500);
    });
});
! function (t) {
    "use strict";
    var e = function () {
        this.nowDate = new Date, this.params = {
            countDownDiff: Math.ceil(86400 - (60 * this.nowDate.getHours() * 60 + 60 * this.nowDate.getMinutes() + this.nowDate.getSeconds())),
            selectors: {
                countDown: ".landing__countdown"
            }
        }, this.initCountDown(), this.initEvents()
    };
    e.prototype.initEvents = function () {
        this.removeStyleTag(), t(".scrollto").on("click", this.scrollToForm)
    }, e.prototype.initCountDown = function () {
        var e = this,
            n = new Date(this.nowDate.getTime() + 1e3 * this.params.countDownDiff);
        setInterval(function () {
            var o = new Date(n.getTime() - Date.now()),
                s = '<span class="hours">' + (o.getHours() > 9 ? o.getHours() : "0" + o.getHours()) + '</span><span class="minutes">' + (o.getMinutes() > 9 ? o.getMinutes() : "0" + o.getMinutes()) + '</span><span class="seconds">' + (o.getSeconds() > 9 ? o.getSeconds() : "0" + o.getSeconds()) + "</span>";
            t(e.params.selectors.countDown).html(s)
        }, 1e3)
    }, e.prototype.removeStyleTag = function (e) {
        t(document.head).find("style").remove()
    }, t(function () {
        window.landing = new e
    })
}(jQuery);